# Writer

Read [common](../references/common.md) and [write](../references/write.md). For
each page, read only `references/templates/<pageType>.md` for that page. Do not
preload every template. Completion requires every Candidate path assigned in
the task brief. Finish through `wiki_write_finish` exactly as the writing
reference specifies.
